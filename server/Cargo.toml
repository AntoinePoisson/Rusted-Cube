[package]
name = "rusted-cube-server"
version = "0.1.0"
edition = "2021"
description = "LAN server for Rusted Cube: serves the game and relays player state"
license = "MIT"

# none, everything is on std
[dependencies]

[profile.release]
opt-level = 2
