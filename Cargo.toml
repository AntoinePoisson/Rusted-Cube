[package]
name = "rusted-cube"
version = "0.1.0"
edition = "2021"
description = "A small voxel sandbox written in Rust and WebAssembly"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
console_error_panic_hook = "0.1.7"
glam = "0.21.3"
js-sys = "=0.3.64"
# pinned, newer releases need a newer compiler than 1.67
wasm-bindgen = "=0.2.87"

# the server is its own crate
[workspace]
exclude = ["server"]

[dependencies.web-sys]
version = "=0.3.64"
features = [
    "console",
    "Document",
    "Element",
    "ErrorEvent",
    "Event",
    "EventTarget",
    "HtmlCanvasElement",
    "HtmlElement",
    "KeyboardEvent",
    "Location",
    "MessageEvent",
    "MouseEvent",
    "Performance",
    "PointerEvent",
    "WebSocket",
    "WebGl2RenderingContext",
    "WebGlBuffer",
    "WebGlProgram",
    "WebGlShader",
    "WebGlUniformLocation",
    "WebGlVertexArrayObject",
    "Window",
]

[profile.release]
codegen-units = 1
lto = true
# speed over size, meshing and noise dominate the frame budget
opt-level = 3
strip = true
